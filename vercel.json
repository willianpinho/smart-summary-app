{
  "buildCommand": "corepack enable && cd frontend && pnpm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "corepack enable && cd frontend && pnpm install",
  "framework": "nextjs"
}
